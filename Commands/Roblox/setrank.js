module.exports.run = (client, message, args) => {
    
}

module.exports.config = {
    name: "setrank",
    aliases: ["sr"],
    module: "roblox",
    usage: "s!setrank <user> <rank>",
    description: "Set's the rank of the user specified to the rank given."
}